import MainContainer from "./MainContainer";
import SideBar from "./SideBar";

const Body = () => {
  return (
    <div>
      <p>Body</p>
      <SideBar />
      <MainContainer />
    </div>
  );
};

export default Body;
